mutation($status : String!,$stepId : String!,$testRunId : String!) {
    updateTestRunStepStatus(
        testRunId: $testRunId,
        stepId: $stepId,
        status: $status
    ) {
        warnings
    }
}